# 项目5：C++ 测绘内业处理程序（控制网平差）· 方案

## 1. 项目概述

用 C++ 实现一个**控制网平差计算程序**：核心计算库（矩阵、平差）仅依赖标准库并自研矩阵库，外层提供 **Qt6 Widgets 可视化窗口**。程序读入控制网的已知点与观测值，输出待定点的平差坐标/高程、观测值改正数及精度评定结果。

定位：课程设计/个人项目的重做版本，替代简历中「C++测绘内业处理程序」条目，突出**数据结构设计**、**自研矩阵运算**、**最小二乘平差**与**可视化界面**能力。

## 2. 目标与范围

- **网型支持**：高程网（水准网）；平面网（导线网、边角网、三角网、三边网）。
- **平差方法**：以**间接平差（参数平差）**为核心；条件平差作为可选扩展。
- **精度评定**：单位权中误差、待定点坐标/高程中误差、平差值函数中误差。
- **数据接口**：文本文件读入（已知点 + 观测值），输出平差报告。
- **模块化**：网型与平差方法解耦，新增网型/观测类型不改动核心算法。
- **可视化界面**：Qt6 Widgets 窗口，网图展示 + 结果表格 + 交互式运行平差。

## 3. 技术选型

| 项 | 选型 |
|---|---|
| 语言标准 | C++17 |
| 构建 | CMake（g++ / MSVC） |
| 依赖 | 核心计算库仅标准库（矩阵自研）；GUI 层依赖 Qt6 Widgets |
| 测试 | 断言 + 教材算例回归 |

## 4. 数据结构设计

| 类型 | 职责 |
|---|---|
| `Point` | 点名、坐标（x/y/h）、已知/待定标记 |
| `Observation`（基类） | 观测值通用接口（起终点、观测值、精度/权） |
| `AngleObs` / `DistanceObs` / `HeightDiffObs` | 角度、边长、高差观测（派生类） |
| `ControlNetwork` | 点集 + 观测集 + 已知点集合 |
| `Matrix` / `Vector` | 自研矩阵库 |

设计要点：`Observation` 提供虚函数用于构造误差方程系数行与常数项，新增观测类型只需新增派生类，核心平差逻辑保持不变。

## 5. 自研矩阵库

- **存储**：一维 `std::vector<double>` 按行主序。
- **运算**：加法、减法、数乘、矩阵乘法、转置。
- **求逆**：高斯-约当消元（含列主元选取）；对称正定矩阵可选 LDL^T 分解。
- **解方程组**：法方程 `N·x̂ = t` 的求解（对称正定优先用 Cholesky/LDL^T）。

## 6. 平差算法（间接平差）

1. 建立误差方程 `V = B·x̂ − l`；对角度、边长等非线性观测按泰勒展开线性化。
2. 构造权阵 `P`（按观测中误差定权：`p_i = σ0² / σi²`）。
3. 组成法方程 `N = Bᵀ P B`，`t = Bᵀ P l`，解 `x̂ = N⁻¹ t`。
4. 迭代计算，直至坐标改正数满足收敛条件。

## 7. 精度评定

- 单位权中误差：`σ0 = √( Vᵀ P V / (n − t) )`（`n` 观测数、`t` 必要观测数）。
- 待定点协因数阵 `Q_xx = N⁻¹`，据此求各待定点坐标/高程中误差。
- 平差值函数 `φ` 的中误差（误差传播律）。

## 8. 目录结构

```
proj_5_cpp_adjustment/
├── CMakeLists.txt
├── include/            # 核心计算库头文件（matrix/point/observation/network/adjustment）
├── src/                # 核心计算库实现 + 文件解析
├── gui/                # Qt6 Widgets 界面（主窗口、网图视图、结果表格）
├── app/                # 程序入口（main.cpp，可 --cli 走命令行）
├── tests/              # 矩阵库单测 + 平差回归测试
└── data/               # 样例输入（已知点、观测值）+ 期望输出
```

## 9. 输入输出格式

### 输入（标准水准网数据，单文件）

```
已知点数  待定点数  观测高差数
已知点编号  已知高程(m)                  # 已知点数 行
起点  终点  高差观测值(m)  路线长度(km)   # 观测高差数 行
```

- 已知点与待定点统一编号，待定点行不出现高程。
- 定权：以 1 km 路线高差中误差为单位权中误差，权 `P_i = 1 / S_i`（`S_i` 为路线长度，km）。

示例：

```
2 2 3
A 152.345
B 158.120
A P1 2.345 1.0
P1 P2 1.234 1.5
B P2 3.456 2.0
```

> 平面网（导线/边角网等）的输入格式另行定义，此处先按标准水准网实现。

### 输出（平差报告）

- 待定点高程平差值及中误差、观测高差改正数、单位权中误差、法方程摘要。

## 10. 可视化窗口界面（Qt6 Widgets）

- **界面布局**
  - 菜单/工具栏：打开数据文件、运行平差、导出报告。
  - 左侧「网图区」：`QGraphicsView` 绘制控制网，已知点与待定点不同样式，观测线标注高差与路线长度。
  - 右侧「结果区」：`QTableView` 分页展示待定点高程平差值及中误差、观测高差改正数、单位权中误差。
  - 底部状态栏：显示文件路径、观测数、单位权中误差。
- **交互流程**：打开标准水准网文件 → 自动布局并绘制网图 → 点击「运行平差」→ 计算核心返回结果 → 表格与网图联动刷新。
- **架构约束**：GUI 仅调用核心库接口、不耦合算法；核心库保持无 Qt 依赖，可独立单测与命令行运行。

## 11. 实施步骤

1. 自研矩阵库 + 单元测试（加法、乘法、转置、求逆、解方程）。
2. 数据结构 + 解析标准水准网数据文件（已知点高程、高差观测与路线长度）。
3. 水准网间接平差端到端跑通（验证整体流程）。
4. 精度评定 + 报告输出。
5. Qt6 可视化窗口（网图绘制 + 结果表格 + 交互运行平差）。
6. 平面网（导线/边角网）线性化迭代平差。
7. 教材算例验证 + 回归测试。

## 12. 验证方案

- 选取测绘教材算例（水准网、导线网各一），将程序平差结果与教材参考答案比对，坐标/高程改正数与中误差应在合理舍入误差内。
